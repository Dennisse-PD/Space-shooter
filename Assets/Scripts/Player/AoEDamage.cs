using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class AoEDamage : MonoBehaviour
{
    //this script is an alt way to damage enemies with the AoE besides the previously used moth.
    //This one is more simplistic so I wanted to try it out.
    // Start is called before the first frame update
    void Start()
    {

    }

    // Update is called once per frame
    void Update()
    {

    }
}